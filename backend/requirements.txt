# --- Core (installed + verified in Phase 1) ---
fastapi==0.139.2
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
pgvector==0.5.0
python-multipart==0.0.32
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1

# --- Reasoning (installed + verified in Phase 2) ---
anthropic==0.119.0

# --- RAG grounding (installed + verified in Phase 4) ---
# Local embeddings (fastembed/onnxruntime, no torch) instead of a hosted
# embeddings API -- see README for why this deviates from the brief's
# LangChain-based retrieval layer.
fastembed==0.8.0

# --- Orchestration (installed + verified in Phase 5) ---
# langchain-core comes along transitively as langgraph's internal dependency
# (message/state types) -- the full langchain package itself is unused here;
# Phase 4's RAG layer and Phase 5's policy engine both call the Claude
# service module directly (see README deviations).
langgraph==1.2.9
langgraph-checkpoint-postgres==3.1.0

# --- Schema validation / observability (added + version-pinned when Phase 5 lands) ---
guardrails-ai
langfuse

# --- FHIR export (added + version-pinned when Phase 8 lands) ---
fhir.resources
