# --- Core (installed + verified in Phase 1) ---
fastapi==0.139.2
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
pgvector==0.5.0
python-multipart==0.0.32
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1

# --- Reasoning (installed + verified in Phase 2) ---
anthropic==0.119.0

# --- RAG grounding (installed + verified in Phase 4) ---
# Local embeddings (fastembed/onnxruntime, no torch) instead of a hosted
# embeddings API -- see README for why this deviates from the brief's
# LangChain-based retrieval layer.
fastembed==0.8.0

# --- Orchestration (installed + verified in Phase 5) ---
# langchain-core comes along transitively as langgraph's internal dependency
# (message/state types) -- the full langchain package itself is unused here;
# Phase 4's RAG layer and Phase 5's policy engine both call the Claude
# service module directly (see README deviations).
langgraph==1.2.9
langgraph-checkpoint-postgres==3.1.0

# --- Terminology normalization (installed + verified in Phase 6) ---
# (pgvector + fastembed above cover the embedding search; no new package)

# --- SOAP compilation + review (installed + verified in Phase 7) ---
# (LangGraph's interrupt()/Command() are already part of langgraph above)

# --- FHIR export (installed + verified in Phase 8) ---
# R4B, the actively-maintained successor namespace in this library -- see
# README deviations. Composition/Observation/Condition/DocumentReference are
# unchanged in content between R4 and R4B.
fhir.resources==8.3.0
# fhir_core (fhir.resources' dependency) imports annotated_types.SLOTS, which
# annotated_types 0.8.0 removed without fhir_core pinning against it --
# pinned here to the last version that still has it.
annotated_types==0.7.0
